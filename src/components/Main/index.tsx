import { Flex, Split } from '@/components';

import * as S from './styles';

const Main = () => (
  <S.MainContent>
    <Flex>
      <Split splitChildren="Boilerplate">Next</Split>
    </Flex>
  </S.MainContent>
);

export default Main;
